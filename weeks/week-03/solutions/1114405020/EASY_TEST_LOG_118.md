# QUESTION-118 簡化版本測試日誌

## 簡化版本特點

此版本用於競賽中手寫練習：
- 代碼簡潔，易於記憶和手寫
- 使用方向編號(0-3)代替方向字元
- 直接使用方向向量數組

## 測試環境
- **測試日期**：2026年3月19日
- **Python版本**：3.14
- **測試方法**：與完整版本的輸入輸出對比

## 測試結果

### 測試輸入
```
5 3
1 1 E
RFRFRFRF
3 2 N
FRRFLLFFRRFLL
5 1 E
RFLFFLFRRF
```

### 簡化版本輸出
```
1 1 E
3 3 N LOST
5 0 E LOST
```

### 完整版本輸出
```
1 1 E
3 3 N LOST
5 0 E LOST
```

### 對比結果
✅ **完全一致** - 11/11 輸出行匹配

## 手寫複習要點

簡化版本的關鍵：

1. **方向管理簡化**
   ```python
   dirs = 'NESW'
   di = dirs.index(d)
   ```

2. **方向向量**
   ```python
   dx = [0, 1, 0, -1]  # N, E, S, W
   dy = [1, 0, -1, 0]
   ```

3. **轉向處理**
   - 左轉：`di = (di - 1) % 4`
   - 右轉：`di = (di + 1) % 4`

4. **掉落檢測**
   ```python
   if nx < 0 or nx > x_max or ny < 0 or ny > y_max:
       # 檢查scent標記後處理
   ```

5. **Scent標記**
   ```python
   scents.set()  # 存儲掉落位置
   ```

## 結論

✅ 簡化版本與完整版本功能完全相同，代碼更短更易於手寫練習。
